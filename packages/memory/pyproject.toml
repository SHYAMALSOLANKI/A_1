[tool.poetry]
name = "memory"
version = "0.1.0"
description = "Database and vector retrieval for PB2A"
authors = ["Admin <admin@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
sqlalchemy = "^2.0.23"
asyncpg = "^0.29.0"
pgvector = "^0.2.4"
pydantic = "^2.5.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
